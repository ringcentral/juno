import { RcClasses } from '../../../../foundation';
import { InnerSuggestionListProps } from '../SuggestionList';

export const RcSuggestionListClasses = RcClasses<InnerSuggestionListProps>(
  ['root', 'expanded', 'toggle'],
  'RcSuggestionList',
);
