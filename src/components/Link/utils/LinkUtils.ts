import { RcTypographyKeys, UnitMap } from '../../../foundation';
import { RcLinkSize } from '../Link';

// TODO: that will be remove, when switch to variant
export const RcLinkTypographies: UnitMap<RcLinkSize, RcTypographyKeys> = {
  small: 'caption1',
  medium: 'body1',
  large: 'headline1',
};
