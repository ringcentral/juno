import { RcClasses } from '../../../../foundation';
import { RcCheckboxProps } from '../Checkbox';

export const RcCheckboxClasses = RcClasses<RcCheckboxProps>(
  ['root', 'disabled', 'checked'],
  'RcCheckbox',
);
