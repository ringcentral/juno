<!-- This file generate automatically, should not modify that directly -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="API Reference/Hooks/useForkRef" />

## useForkRef

### Variables

- [useForkRef](#const-useforkref)

## Variables

### `Const` useForkRef

• **useForkRef**: _useForkRef_ = MuiUseForkRef

That method will give you ability fork same value `ref object` or `method`
Example:

```ts
const refA = useRef();
const refB = useRef();

const ref = useForkRef(refA, refB);

useEffect(() => {
  console.log(refA); // get that current div dom
  console.log(refB); // also get that current div dom
}, []);

return <div ref={ref} />;
```
