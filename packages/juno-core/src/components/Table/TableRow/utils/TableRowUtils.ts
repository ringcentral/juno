import { RcClasses } from '../../../../foundation';
import { RcTableRowProps } from '../TableRow';

export const RcTableRowClasses = RcClasses<RcTableRowProps>(
  ['selected', 'hover', 'disabled'],
  'RcTableRow',
);
