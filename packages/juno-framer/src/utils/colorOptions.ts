import {
  textPaletteChoice,
  paletteChoice,
} from '@ringcentral/juno-storybook/src/utils/paletteChoice';

export const colorOptions = paletteChoice;

export const textColorOptions = textPaletteChoice;
